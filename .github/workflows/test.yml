name: Dummy workflow

on:
  push:
    branches:
      - main


env:
  DESIGNITE_OUTPUT: /tmp/Desingnite_Output_${{ github.sha }}

jobs:
  test:
    runs-on: ubuntu-latest      
    steps:
      # - uses: actions/checkout@v2
      # - uses: actions/setup-python@v2
      #   with:
      #     python-version: 3.8
      # - uses: actions/cache@v2
      #   with:
      #     path: ~/.cache/pip
      #     key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
      #     restore-keys: |
      #       ${{ runner.os }}-pip-
      # - run: pip install -r requirements.txt
      # - run: pytest
      # - run: ${{ github.event }}
      # - run: echo ${{ github.event.base_ref }}
      # - run: echo ${{ github.event.head_ref }}
      # - run: echo ${{ github.event.before }}
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"

      - uses: actions/setup-java@v1
        with:
          java-version: 17
          distribution: 'corrretto'
      
      - name: Checkout
        uses: actions/checkout@v4
        
        
      - name: Run Designite
        id: designite
        run: |
            java --version
            echo "${{env.DESIGNITE_OUTPUT}}"
            java -jar ./.github/DesigniteJava.jar -i ./ -o "${{env.DESIGNITE_OUTPUT}}" -d
      
      - name: Archive Designite results
        uses: actions/upload-artifact@v3
        with:
          name: designite-output-${{ github.sha }}
          path: "${{env.DESIGNITE_OUTPUT}}"
 

  compare:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Test Action
        uses: IP1102/designite-diff-action@097bf85a0080676cd1ad69079e6a029238b33f0d
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          designite-output: ${{ env.DESIGNITE_OUTPUT }}
          run-id: ${{ github.run_id }}    


      